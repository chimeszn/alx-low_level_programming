#include <stdio.h>

/**
 * main - entry point
 * puts - prints the phrase 'Programming is like building a multilingual puzzle'
 * return - 0 if exited properly, non-zero if otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");	  
	return (0);
}
